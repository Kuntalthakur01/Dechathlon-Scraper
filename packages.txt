sudo apt-get update
sudo apt-get install -y chromium-browser
sudo apt-get install -y chromium-chromedriver

# Install the required Python packages
pip install altair==5.2.0 attrs==23.2.0 blinker==1.7.0 ... (continue with the rest)

# Install Chrome and ChromeDriver versions using pip
pip install chromedriver-autoinstaller==0.6.3
pip install google-chrome-stable==<version>
pip install chromedriver==109.0.5414.74

# Additional packages
pip install streamlit selenium seleniumbase webdriver-manager
